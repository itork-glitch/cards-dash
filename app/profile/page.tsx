import React from 'react';
import { Profile } from '@/components';

export const metadata = {
  title: 'profile',
};

const page = () => {
  return (
    <section>
      <Profile />
    </section>
  );
};

export default page;
